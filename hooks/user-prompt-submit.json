{
  "description": "claude-code-now — injects precise timestamps and elapsed time on every prompt",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/hook.sh || true"
          }
        ]
      }
    ]
  }
}
